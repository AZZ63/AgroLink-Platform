spring:
  datasource:
    url: jdbc:mysql://mysql:3306/agrolink?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: root
  data:
    redis:
      host: redis
      port: 6379
